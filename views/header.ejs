<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="/">
        <img src="/image/logo.png" alt="" class="logo">
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li <% if (name === 'index') { %> class="active" <% } %>><a href="/">首页</a></li>
        <li <% if (name === 'list') { %> class="active" <% } %>><a href="/list">有物</a></li>
        <li <% if (name === 'question') { %> class="active" <% } %>><a href="/question">有求</a></li>
        <% if (user) { %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">用户: <%= user.name %><span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="###">消息（1）</a></li>
              <li><a href="/logout">登出</a></li>
            </ul>
          </li>
        <% } else { %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">用户 <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="/login">登录</a></li>
              <li><a href="/register">注册</a></li>
            </ul>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>